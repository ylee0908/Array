def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        res = []
        d = dict()
        for n in nums1:
            if n in d:
                d[n] += 1
            else:
                d[n] = 1
        
        for n in nums2:
            if n in d and d[n]>0:
                res.append(n)
                d[n] = 0
        return res
