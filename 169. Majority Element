def majorityElement(self, nums: List[int]) -> int:
        d = dict()
        for i in range(len(nums)):
            if nums[i] in d:
                d[nums[i]] += 1
            else:
                d[nums[i]] = 1
        for k in d:
            if d[k] > len(nums)/2:
                return k
        
